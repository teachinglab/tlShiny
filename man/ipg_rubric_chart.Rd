% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipg_grade.R
\name{ipg_rubric_chart}
\alias{ipg_rubric_chart}
\title{Faceted Bar Chart of IPG Domain Scores Across Timepoints}
\usage{
ipg_rubric_chart(data, rubric, colors)
}
\arguments{
\item{data}{Long-format data from \code{\link{ipg_summarise_overall}} containing columns:
\code{direct_to_ts_obs}, \code{Core Action}, \code{Score}, and \code{n}.}

\item{rubric}{Character string used in the plot title (e.g., rubric name shown to users).}

\item{colors}{Integer for the number of colors to request from \code{tlShiny::tl_palette}; choose
a value \eqn{\ge} 8 to cover all categories.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots domain-level IPG scores (CA1–CA3, AC, AD, SP, TDI, and Overall) with facets by
observation timepoint. Labels bars with percent and sample size (n). Uses \code{tlShiny::tl_palette}
for fills and abbreviates domain labels in the plot.
}
\examples{
teacher_scores <- ipg_summarise_teacher(ipg_data)
overall_long <- ipg_summarise_overall(teacher_scores)
ipg_rubric_chart(overall_long, rubric = "Math IPG", colors = 8)
}
